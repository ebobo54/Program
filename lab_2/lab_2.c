#include<math.h>
#include<stdio.h>
int main() {
    double x = 0,y;
    double eps;
    double h;
    scanf("%lf"&h);
    eps=h/2;
    printf("x\t\ty\n");
    
    while(0<=x && x<=1){
        cos(x+pow(x,3);
        printf("%f\t%f\n",x,y);
    }
    while(1<x && x<=2){
        exp(pow(-x,2)-pow(x,2)+2x';
        printf("%f\t%f\n",x,y);
    }
    return 0
}
