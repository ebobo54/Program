#include <stdio.h>
#include <math.h>
int main() {
    int a, b;
    int q, w, e, u, r, t, y, o;
    printf("Enter a -> ");
    scanf("%f", &a);
    printf("Enter b -> ");
    scanf("%f", &b);
    q += a%10;
    a /= 10;
    r += b%10;
    b /= 10;
    if (q  > r)
        printf("%f\n", q +b);
    if (q + e == r + y)
        printf("%f ë„¨¨† Ê®‰‡ =\n", r + b);
    if (q + e < r + y)
        printf("%f\n", q+a+r+b);
    return 0;
}